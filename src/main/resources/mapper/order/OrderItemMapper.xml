<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ko.dh.goot.order.dao.OrderItemMapper">


<select id="selectOrderItemByOrderId" parameterType="long" resultType="OrderItem">

    SELECT
        oi.order_item_id,
        oi.order_id,
        oi.product_id,
        oi.option_id,
        oi.unit_price,
        oi.quantity,
        oi.total_price,

        p.product_name,
        po.color,
        po.size,
        oi.refund_status,
        oi.created_at
    FROM order_item oi
    JOIN product p ON oi.product_id = p.product_id
    JOIN product_option po ON oi.option_id = po.option_id
    WHERE oi.order_id = #{orderId}

</select>


<insert id="insertOrderItem" parameterType="OrderItemRecord"
            useGeneratedKeys="true" keyProperty="orderItemId">

        INSERT INTO order_item (
            order_id,
            product_id,
            option_id,
            product_name,
            unit_price,
            quantity,
            total_price,
            color,
            size,
            refund_status
        ) VALUES (
            #{orderId},
            #{productId},
            #{optionId},
            #{productName},
            #{unitPrice},
            #{quantity},
            #{totalPrice},
            #{color},
            #{size},
            #{refundStatus}
        )
</insert>

</mapper>
